[project]
name = "rag-document-engine"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "numpy>=1.26.0",
]

[tool.setuptools]
packages = []
